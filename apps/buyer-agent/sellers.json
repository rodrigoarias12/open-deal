{
  "_comment": "List of seller ENS subnames. Endpoint + address are resolved at runtime from ENS text records (key: 'endpoint') and addr coinType=60. Anyone can register their own subname under .openagents-treasury.eth and become discoverable.",
  "sellers": [
    "seller-acme.openagents-treasury.eth"
  ]
}
